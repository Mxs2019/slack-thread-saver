# Slack App Credentials
# Get these from https://api.slack.com/apps

# Bot User OAuth Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Signing Secret (found in Basic Information)
SLACK_SIGNING_SECRET=your-signing-secret

# Server Port (optional, defaults to 3000)
PORT=3000

# Environment (optional)
NODE_ENV=development

# ===========================================
# WEBHOOK MODE WITH NGROK
# ===========================================
# This app now runs in webhook mode (not socket mode)
# 
# To use with ngrok:
# 1. Start the app: npm run dev
# 2. In another terminal: ngrok http 3000
# 3. Copy the HTTPS URL from ngrok (e.g., https://abc123.ngrok.io)
# 4. Update your Slack app settings at https://api.slack.com/apps:
#    - Event Subscriptions: 
#      Enable and set Request URL to: https://YOUR-NGROK-URL.ngrok.io/slack/events
#    - Interactivity & Shortcuts:
#      Enable and set Request URL to: https://YOUR-NGROK-URL.ngrok.io/slack/events
#    - Slash Commands:
#      Set Request URL to: https://YOUR-NGROK-URL.ngrok.io/slack/events
# 5. Save changes and Slack will verify the URLs